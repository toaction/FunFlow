server:
  port: 8080

spring:
  # 数据源配置（使用 Druid 连接池）
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/fun_flow?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: 12345678
  
  # Redis 配置
  data:
    redis:
      host: localhost
      port: 6379
      password: # 如果有密码，填写密码
      database: 0
      timeout: 3000ms
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
          max-wait: -1ms
  
  # 邮件配置
  mail:
    host: smtp.163.com
    port: 465
    username: liuxiangfu2002@163.com
    password: ZJrRU3ApdfBpYUhj # SMTP 授权码
    default-encoding: UTF-8
    properties:
      mail:
        smtp:
          auth: true
          ssl:
            enable: true
          connectiontimeout: 10000
          timeout: 10000
          writetimeout: 10000

# MyBatis 配置
mybatis:
  type-aliases-package: com.action.domain.entity
  configuration:
    map-underscore-to-camel-case: true

